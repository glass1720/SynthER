from setuptools import setup, find_packages

setup(
    name='synther',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # any dependencies your project has, e.g., 'requests'
    ],
)